import 'package:flutter/material.dart';

class CommonButtons extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Common Buttons')),
      body: Center(child: Text('Common Buttons Content')),
    );
  }
}