import 'package:flutter/material.dart';

class SliderScreen extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return Scaffold(
            appBar: AppBar(
                title: Text('Slider Screen'),
            ),
            body: Center(
                child: Text('This is the Slider Screen'),
            ),
        );
    }
}