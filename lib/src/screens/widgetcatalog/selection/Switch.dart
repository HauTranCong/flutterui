import 'package:flutter/material.dart';

class SwitchScreen extends StatelessWidget {
    @override
    Widget build(BuildContext context) {
        return Scaffold(
            appBar: AppBar(
                title: Text('Switch Screen'),
            ),
            body: Center(
                child: Text('This is the Switch Screen'),
            ),
        );
    }
}